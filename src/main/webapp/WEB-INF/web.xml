<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <!-- Filtre pour la gestion des rôles -->
    <filter>
        <filter-name>RoleFilter</filter-name>
        <filter-class>com.gestion.location.web.filter.RoleFilter</filter-class>
    </filter>

    <filter-mapping>
        <filter-name>RoleFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Page d’accueil par défaut -->
    <welcome-file-list>
        <!-- Si tu veux ouvrir directement la JSP -->
        <!-- <welcome-file>auth/login.jsp</welcome-file> -->

        <!-- Si tu veux passer par ton LoginServlet -->
        <welcome-file>login</welcome-file>
    </welcome-file-list>

</web-app>
